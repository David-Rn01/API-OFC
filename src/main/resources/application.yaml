spring:
  application:
    name: API_OFC

  datasource:
    url: jdbc:mysql://localhost:3306/seu_banco?useSSL=false&serverTimezone=UTC
    username: root
    password: Ppandacell-123
    driver-class-name: com.mysql.cj.jdbc.Driver  # ← correto

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true